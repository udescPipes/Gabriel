# Operadores aritméticos (+, -, /, *, **)
a <- 5
b <- 2

soma <- a + b
subt <- a - b
mult <- a * b
div <- a / b
pot <- a ** 2
pot <- a ^ b

#Operadores relacionais (==, !=, >, < , >=, <= )
a == b
a != b 
a > b 
a >= b 
a < b 
a <= b

#Operadores Lógicos (AND - &, OR - |, NOT - !)
a == b & a > b #Operador E
a == b | a > b #Operador Ou
!(a == b) && a > b #Operador Negação
